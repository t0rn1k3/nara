// Europe projection + feature extraction — implemented in the map step.
export const EUROPEAN_COUNTRY_IDS = new Set<number>();

export function getCountryPaths(): { d: string; isEuropean: boolean }[] {
  return [];
}
